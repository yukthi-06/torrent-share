# User Prompt
```text
In the share tab, make the gap between lines compact, so that the screen vertical height can be reduced.
```

# Tasks - Compact Share Tab Layout

- [x] Modify `ShareTab.java` in `buildUI` to reduce layout spacing and padding (root, dragZone, infoPanel, grid, magnetBox, etc.).
- [x] Modify `ShareTab.java` in `setupDragAndDrop` to update active and normal drag zone style paddings to `20px`.
- [ ] Compile and verify the Java project.
