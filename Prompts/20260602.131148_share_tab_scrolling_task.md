# User Prompt
```text
In share File tab, there is no vertical scroll bar.  When resized, bottom parts are not visible
```

# Tasks - Add Scrollability to Share Files Tab

- [x] Import `Parent` and `ScrollPane` in `ShareTab.java`.
- [x] Initialize `ScrollPane` in `ShareTab.java` and wrap the `VBox` root.
- [x] Modify `getContent()` to return the `ScrollPane` Parent.
- [ ] Compile and verify the Java project.
