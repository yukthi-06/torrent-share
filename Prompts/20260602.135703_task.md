# User Prompt
```text
Even thought trackers.properties have 1 tracker urls, the settings panel shows only 4 values.  fix it
```

# Tasks

- [ ] Modify database seeding logic in `DatabaseManager.java` to synchronize default trackers from `trackers.properties` on startup.
- [ ] Build the project using Gradle (`.\gradlew.bat compileJava`) to ensure compilation is successful.
- [ ] Verify that trackers sync works.
