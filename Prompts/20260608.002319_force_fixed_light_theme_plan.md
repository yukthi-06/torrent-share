# User Prompt
dont depend on system theme or system color.
Force colors and siaplay the UI, so that it looks same on all systems

# Goals
Remove system theme detection and force a consistent light color theme across all systems.

## Proposed Changes

### Styling (CSS)

#### [MODIFY] [index.css](file:///x:/Projects_X/0_Active/1_Java_Active/Torrent_Share_Java_GITHUB/src/main/resources/index.css)
- Explicitly set `-fx-background-color: #f9fafb;` on `.root` to guarantee a consistent background color regardless of the operating system's default JavaFX background.

### UI Coordinator

#### [MODIFY] [MainWindow.java](file:///x:/Projects_X/0_Active/1_Java_Active/Torrent_Share_Java_GITHUB/src/main/java/com/vypeensoft/torrentshare/ui/MainWindow.java)
- Remove `ThemeDetector.isDarkMode()` logic and the conditional addition of the `dark-theme` style class so the light theme is always applied.
- Remove the unused import of `com.vypeensoft.torrentshare.util.ThemeDetector`.

## Verification Plan

### Manual Verification
- Compile and run the application.
- Verify that the application uses the forced light theme regardless of system dark/light mode preferences.
