# User Prompt
> change java package from "com.torrentshare" t o "com.vypeensoft.torrentshare"

---

# TorrentShare Package Refactor Implementation Plan

We will perform a complete refactoring of the application's root package name from `com.torrentshare` to `com.vypeensoft.torrentshare`.

## Proposed Changes

1. **Move Java Source Files**:
   - Create new directory path `src/main/java/com/vypeensoft/torrentshare`.
   - Move all directories and source files under `src/main/java/com/torrentshare/` to `src/main/java/com/vypeensoft/torrentshare/`.
   - Delete the old empty `src/main/java/com/torrentshare` folder structure.

2. **Refactor Package Declarations and Imports**:
   - Update every `.java` file under the new directory structure:
     - Replace package declaration `package com.vypeensoft.torrentshare...` with `package com.vypeensoft.torrentshare...`
     - Replace all imports of the form `import com.vypeensoft.torrentshare...` with `import com.vypeensoft.torrentshare...`

3. **Update Build Configuration**:
   - In [build.gradle](file:///x:/Projects_X/0_Active/1_Java_Active/Torrent_Share_Java_GITHUB/build.gradle):
     - Update `group = 'com.torrentshare'` to `group = 'com.vypeensoft.torrentshare'`
     - Update `mainClass = 'com.vypeensoft.torrentshare.Launcher'` to `mainClass = 'com.vypeensoft.torrentshare.Launcher'`
     - Update the custom fat JAR `Main-Class` manifest attribute from `com.vypeensoft.torrentshare.Launcher` to `com.vypeensoft.torrentshare.Launcher`.

## Refactoring Execution Script

We will use a series of automated PowerShell command lines to create the new package directory structure, move all directories, and execute global inline text replacements on all `.java` files for fast, zero-error execution.

## Verification Plan

1. **Compile Verification**:
   - Run `gradlew compileJava` to guarantee that all packages and imports are fully resolved and compile successfully.
2. **Fat JAR packaging verification**:
   - Run `gradlew jar` to build the single executable fat JAR under the new package hierarchy.
