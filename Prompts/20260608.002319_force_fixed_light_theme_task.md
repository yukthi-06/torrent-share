# User Prompt
dont depend on system theme or system color.
Force colors and siaplay the UI, so that it looks same on all systems

# Tasks

- [x] Update `index.css` to set a fixed background color on `.root`.
- [x] Remove system theme detection in `MainWindow.java` and clean up imports.
