# User Prompt
* Where:
Build file 'X:\Projects_X\0_Active\1_Java_Active\Torrent_Share_Java_GITHUB\build.gradle' line: 4

* What went wrong:
Plugin [id: 'org.openjfx.javafxplugin', version: '0.10.0'] was not found in any of the following sources:

- Gradle Core Plugins (plugin is not in 'org.gradle' namespace)
- Plugin Repositories (could not resolve plugin artifact 'org.openjfx.javafxplugin:org.openjfx.javafxplugin.gradle.plugin:0.10.0')
  Searched in the following repositories:
    Gradle Central Plugin Repository

---

build for windows only.  No need for macos or linux, or ARM architecture

---

# TorrentShare Build System Adjustments Plan

This document outlines the modifications made to the build configuration in response to plugin resolution failures and cross-platform targets exclusion requests.

## Proposed Changes

### Build and Configurations

#### [MODIFY] [build.gradle](file:///x:/Projects_X/0_Active/1_Java_Active/Torrent_Share_Java_GITHUB/build.gradle)
We have updated the build script to:
1. Fix the `org.openjfx.javafxplugin` version by downgrading it to `0.1.0` (which is fully resolved by the official Gradle Plugin Portal).
2. Remove native `jlibtorrent` dependency binaries targeting non-Windows platforms (specifically Linux x86_64, macOS Apple Silicon ARM64, and macOS Intel x86_64), keeping only the Windows-specific DLL native integration wrappers.

```diff
 plugins {
     id 'java'
     id 'application'
-    id 'org.openjfx.javafxplugin' version '0.10.0'
+    id 'org.openjfx.javafxplugin' version '0.1.0'
 }
 
 group = 'com.torrentshare'
@@ -24,9 +24,7 @@
     // Core jlibtorrent bindings and platform-specific native libraries
     implementation 'com.frostwire:jlibtorrent:2.0.13.6'
     implementation 'com.frostwire:jlibtorrent-windows:2.0.13.6'
-    implementation 'com.frostwire:jlibtorrent-linux-x86_64:2.0.13.6'
-    implementation 'com.frostwire:jlibtorrent-macosx-arm64:2.0.13.6'
-    implementation 'com.frostwire:jlibtorrent-macosx-x86_64:2.0.13.6'
+

     // SQLite JDBC Driver
     implementation 'org.xerial:sqlite-jdbc:3.45.1.0'
```

## Verification Plan

### Automated Verification
Run compilation to confirm build parameters match valid dependencies:
- `gradle compileJava`

### Manual Verification
Confirm that no macOS or Linux JAR binaries are cached or pulled into runtime builds.
