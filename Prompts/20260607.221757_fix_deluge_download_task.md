# User Prompt
seeding from this app, i am not able to download from popular torrent client Deluge

# Tasks

- [x] Modify `SessionManager.java` to enable `allow_multiple_connections_per_ip` and update `listen_interfaces` to include both IPv4 and IPv6 wildcards.
- [x] Add `getMagnetUri(String infoHash)` to `TorrentManager.java` to query the registered magnet URI from the database.
- [x] Add `getMagnetUri(String infoHash)` to `ShareService.java` that delegates to `TorrentManager`.
- [x] Modify `ShareTab.java` to display the fully populated magnet URI from the database rather than generating a fresh partial one.
- [x] Compile and verify the project using Gradle.
